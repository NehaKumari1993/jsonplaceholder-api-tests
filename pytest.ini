[pytest]
markers =
    smoke: quick sanity checks across all resources (list + get-by-id only)
    positive: full happy-path CRUD coverage (list, get-by-id, filter, create, replace, patch, delete)
    negative: negative / error-path scenarios
    nested: nested resource / relationship routes
addopts = -v --strict-markers
